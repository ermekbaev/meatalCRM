import { redirect } from "next/navigation";

// Кабинет оператора теперь — та же доска задач, что и у мастера (с колонками и
// перетаскиванием) плюс доступ к складу. Старый мобильный список подзадач убран,
// маршрут сохранён и ведёт на доску.
export default function OperatorPage() {
  redirect("/tasks");
}
